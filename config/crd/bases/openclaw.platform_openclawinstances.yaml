---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: openclawinstances.openclaw.platform
spec:
  group: openclaw.platform
  names:
    kind: OpenClawInstance
    listKind: OpenClawInstanceList
    plural: openclawinstances
    singular: openclawinstance
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .spec.userId
      name: UserID
      type: string
    - jsonPath: .spec.plan
      name: Plan
      type: string
    - jsonPath: .status.gatewayEndpoint
      name: Endpoint
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: OpenClawInstance is the Schema for the openclawinstances API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
            type: string
          metadata:
            type: object
          spec:
            description: OpenClawInstanceSpec defines the desired state of OpenClawInstance
            properties:
              config:
                description: Config is the OpenClaw configuration (openclaw.json)
                type: string
              expiresAt:
                description: ExpiresAt is the instance expiration time
                format: date-time
                type: string
              image:
                default: openclaw/openclaw:latest
                description: Image configuration
                type: string
              instanceId:
                description: InstanceID is the unique identifier for this instance
                type: string
              plan:
                default: free
                description: Plan is the subscription plan (free/pro/team)
                type: string
              resources:
                description: Resources specifies compute resources
                properties:
                  cpu:
                    default: 250m
                    description: CPU request (e.g., "250m", "1")
                    type: string
                  cpuLimit:
                    default: 1000m
                    description: CPU limit
                    type: string
                  memory:
                    default: 512Mi
                    description: Memory request (e.g., "512Mi", "2Gi")
                    type: string
                  memoryLimit:
                    default: 2Gi
                    description: Memory limit
                    type: string
                type: object
              userId:
                description: UserID is the owner user ID
                type: string
            required:
            - instanceId
            - userId
            type: object
          status:
            description: OpenClawInstanceStatus defines the observed state of OpenClawInstance
            properties:
              conditions:
                description: Conditions represent the latest observations
                items:
                  description: Condition contains details for one aspect of the current state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in FooBar.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              gatewayEndpoint:
                description: GatewayEndpoint is the full access URL
                type: string
              hostIP:
                description: HostIP is the node IP where the Pod is running
                type: string
              hostPort:
                default: 18789
                description: HostPort is the exposed port (always 18789)
                format: int32
                type: integer
              lastHealthCheck:
                description: LastHealthCheck is the timestamp of last health check
                format: date-time
                type: string
              observedGeneration:
                description: ObservedGeneration is the last reconciled generation
                format: int64
                type: integer
              phase:
                description: Phase is the current lifecycle phase
                enum:
                - Pending
                - Creating
                - Running
                - Stopped
                - Error
                - Deleting
                type: string
              podName:
                description: PodName is the name of the managed Pod
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
